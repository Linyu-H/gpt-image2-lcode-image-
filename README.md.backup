# Lcode Image

<p align="center">
  <img src="./lcode-image-logo.png" alt="Lcode Image Logo" width="120" />
</p>

<p align="center">
  <strong>一个功能完整、开箱即用的 AI 图片生成站</strong>
</p>

<p align="center">
  支持自托管部署 · 用户系统 · 社区分享 · 管理后台 · OAuth 登录
</p>

<p align="center">
  <a href="#中文">中文</a> ·
  <a href="#english">English</a> ·
  <a href="#日本語">日本語</a>
</p>

---

# 中文

## 📖 项目简介

**Lcode Image** 是一个功能完整的 AI 图片生成 Web 应用，专为自托管场景设计。无论你是想搭建个人图片生成站、团队内部工具，还是进行二次开发，Lcode Image 都能满足你的需求。

### ✨ 核心特性

- 🎨 **AI 图片生成**：支持文生图和参考图编辑，兼容 OpenAI 风格接口
- 👥 **完整用户系统**：游客模式、注册登录、邮箱验证、邀请码机制
- 🔐 **OAuth 集成**：支持 Linux.do 一键登录，可扩展其他 OAuth 提供商
- 🌐 **社区功能**：用户可发布作品到社区，浏览他人创作
- 🎭 **AI 头像生成**：用户可上传头像或使用 AI 生成个性化头像
- 🛠️ **强大的管理后台**：配置上游接口、管理用户、图片治理、数据统计
- 📊 **数据可视化**：首页展示站点统计、生成趋势图表
- 🎯 **灵感池系统**：管理员可配置首页示例图和提示词，激发用户创作
- 🔒 **安全可靠**：JWT 鉴权、密码加密、敏感信息加密存储、IP 限流
- 🎨 **精美 UI**：全站宋体设计、明亮/护眼双主题、响应式布局
- 🐳 **一键部署**：提供 Docker Compose 配置，5 分钟完成部署

> Maintained by **lcode**.

## 📸 页面预览

### 🏠 首页 - 精美的落地页设计

![首页预览](./index.png)

首页展示站点核心信息，包括：
- 站点介绍和特色功能说明
- 实时统计数据（总生成次数、用户数、社区帖子数）
- 生成趋势图表（ECharts 可视化）
- 精选示例图片轮播，激发创作灵感
- 快速开始按钮，引导用户体验

### 🎨 开始生成 - 强大的图片生成工具

![开始生成页预览](./make-image.png)

核心生成功能页面，支持：
- **文生图模式**：输入提示词直接生成图片
- **参考图编辑**：上传参考图进行 AI 编辑和变换
- **会话保持**：生成记录自动保存，刷新页面不丢失
- **历史复用**：从历史记录一键复用成功的提示词
- **实时预览**：生成过程中显示加载状态
- **高清下载**：支持下载原图和分享到社区

### 🌐 社区论坛 - 发现和分享创意

![社区页预览](./community-image.png)

用户作品展示和交流平台：
- 瀑布流布局展示社区所有公开作品
- 点击查看作品详情、提示词和生成参数
- 用户可发布自己的作品到社区
- 支持删除自己发布的帖子
- 查看作者信息和发布时间

### 👤 个人主页 - 完整的用户中心

![个人主页预览](./mine.png)

用户个人信息管理中心：
- **个人资料**：修改用户名、邮箱等基本信息
- **头像管理**：上传自定义头像或使用 AI 生成头像
- **API 配置**：配置个人专属的图片生成 API（可选）
- **令牌管理**：设置个人身份令牌，优先级高于共享令牌
- **统计信息**：查看个人总生成次数和账号创建时间
- **历史记录**：查看所有生成历史，支持删除和分享

### 🛠️ 管理后台 - 全面的站点管理

管理员后台提供完整的站点管理能力：

#### 后台首页
- 核心功能入口卡片式导航
- 图片生成统计图表
- 站点概览数据（用户数、图片数、社区帖子数）

#### 配置中心 (`/admin/config`)
集中管理所有运行时配置：
- **上游 API 配置**：图片生成接口地址、模型、参数
- **站点配置**：站点 URL、公告、注册开关
- **邮箱配置**：SMTP 服务器设置，支持邮箱验证码
- **邀请码系统**：批量生成、查看使用情况
- **OAuth 配置**：Linux.do 登录接入设置
- **限流配置**：每日单 IP 生成次数限制
- **清理策略**：自动清理过期图片的 Cron 配置

#### 用户管理 (`/admin/users`)
- 查看所有注册用户列表
- 重置用户密码
- 封禁/解封用户账号
- 查看用户生成统计

#### 图片管理 (`/admin/images`)
- 查看站内所有生成图片
- 按用户、时间筛选图片
- 删除违规或不当图片
- 批量清理过期图片

#### 灵感池管理
- 配置首页展示的精选提示词
- 上传和管理示例图片
- 设置图片轮播顺序

## 🎯 功能详解

### 用户功能

#### 🎨 图片生成
- **文生图**：输入创意提示词，AI 自动生成高质量图片
- **参考图编辑**：上传图片作为参考，AI 根据提示词进行编辑和变换
- **会话保持**：生成过程中的消息和图片自动保存，刷新不丢失
- **历史复用**：从历史记录中一键复用成功的提示词，提高创作效率
- **高清输出**：支持 1024x1024 等多种分辨率，HD 质量输出

#### 👥 账号系统
- **多种注册方式**：
  - 邮箱验证码注册（需配置 SMTP）
  - 邀请码注册（管理员可开启/关闭必填）
  - Linux.do OAuth 一键登录（需管理员配置）
- **游客模式**：未登录用户也可体验生成功能（受限流限制）
- **密码找回**：通过邮箱验证码重置密码
- **安全保障**：bcrypt 密码加密，JWT 令牌鉴权

#### 🖼️ 历史记录
- 查看所有个人生成历史
- 按时间倒序排列，快速找到历史作品
- 支持删除不满意的图片
- 一键分享优秀作品到社区
- 查看每张图片的生成参数和提示词

#### 🌐 社区互动
- 浏览社区所有公开作品
- 查看作品详情页，学习他人的提示词技巧
- 发布自己的作品到社区，获得曝光
- 管理自己发布的帖子，支持删除

#### 👤 个人中心
- **基本信息**：修改用户名、邮箱等资料
- **头像管理**：
  - 上传本地图片作为头像
  - 使用 AI 生成个性化头像（预览后确认）
- **API 配置**（高级功能）：
  - 配置个人专属的图片生成 API 地址
  - 设置个人身份令牌（优先级高于站点共享令牌）
  - 适合有自己 API 资源的用户
- **统计信息**：查看个人总生成次数和账号创建时间

### 管理员功能

#### 🔧 系统配置
- **上游 API 管理**：
  - 配置共享图片生成 API 地址（OpenAI 风格接口）
  - 设置共享身份令牌（加密存储）
  - 配置生成参数（模型、尺寸、质量、风格等）
  - 测试上游接口连通性
- **站点设置**：
  - 配置站点 URL（用于图片链接生成）
  - 发布全站公告（支持 HTML）
  - 开启/关闭用户注册
  - 设置邀请码必填开关
- **邮箱服务**：
  - 配置 SMTP 服务器（支持 QQ 邮箱等）
  - 测试邮件发送功能
  - 用于注册验证码和密码找回

#### 👥 用户管理
- 查看所有注册用户列表
- 查看用户详细信息（注册时间、生成次数等）
- 重置用户密码（用户忘记密码时）
- 封禁/解封用户账号（处理违规用户）
- 查看用户生成统计

#### 🖼️ 图片治理
- 查看站内所有生成图片资源
- 按用户、时间、类型筛选图片
- 删除单张违规或不当图片
- 批量清空生成图片（谨慎操作）
- 手动触发过期图片清理
- 查看图片存储统计

#### 🎫 邀请码系统
- 批量生成邀请码（可设置生成数量）
- 查看邀请码列表和使用状态
- 查看每个邀请码的使用者和使用时间
- 支持邀请码必填或选填模式

#### 🔐 OAuth 接入
- **Linux.do 登录配置**：
  - 填写 Client ID 和 Client Secret
  - 配置回调地址
  - Client Secret 加密存储，确保安全
  - 配置后前端自动显示登录按钮
- **扩展性**：代码结构支持接入其他 OAuth 提供商

#### 🛡️ 安全与限流
- **IP 限流**：配置每日单 IP 最大生成次数，防止滥用
- **自动清理**：配置 Cron 表达式，定时清理过期图片（默认 3 天）
- **敏感信息保护**：所有令牌和密钥加密存储
- **首次登录强制改密**：管理员首次登录必须修改默认密码

#### 🎨 内容管理
- **首页灵感池**：
  - 上传精选示例图片
  - 配置示例提示词
  - 设置图片展示顺序
  - 激发用户创作灵感
- **社区管理**：查看和管理社区帖子

### 系统能力

#### 💾 数据持久化
- **SQLite 数据库**：轻量级、零配置、单文件存储
- **本地文件存储**：图片保存在服务器本地，无需对象存储
- **数据表设计**：
  - `users` - 用户账号信息
  - `user_profiles` - 用户配置和统计
  - `generated_images` - 生成图片记录
  - `generation_logs` - 生成日志
  - `community_posts` - 社区帖子
  - `avatar_previews` - 头像预览
  - `featured_prompts` - 精选提示词
  - `featured_examples` - 示例图片
  - `email_verification_codes` - 邮箱验证码
  - `invite_codes` - 邀请码
  - `admin_settings` - 管理员配置
  - `linuxdo_connect` - OAuth 配置

#### 🔄 自动化任务
- **定时清理**：基于 node-cron 的自动清理机制
- **默认策略**：生成图片 3 天后自动过期删除
- **智能保护**：
  - 用户头像不参与自动清理
  - 社区帖子关联的图片随帖子一起失效
  - 可通过管理后台自定义清理策略
- **手动清理**：管理员可随时手动触发清理任务

#### 🔒 安全机制
- **身份认证**：
  - JWT 令牌鉴权（用户和管理员分离）
  - bcrypt 密码哈希（10 轮加盐）
  - 令牌过期自动刷新
- **数据加密**：
  - 敏感令牌 AES-256-CBC 加密存储
  - OAuth Client Secret 加密保护
  - 环境变量隔离敏感配置
- **访问控制**：
  - 管理员路由中间件保护
  - 用户路由权限验证
  - 跨域请求白名单控制
- **防滥用**：
  - 单 IP 每日生成次数限制
  - 邮箱验证码频率限制
  - Prompt 内容校验

#### 🌐 URL 管理
- **统一 URL 生成**：所有图片 URL 通过 `site_base_url` 统一管理
- **灵活部署**：支持子目录、反向代理、CDN 等多种部署方式
- **路径规范**：`/uploads/` 目录统一存储用户上传和生成的图片

#### ⏰ 时间处理
- **统一格式**：全站时间格式统一为 `yyyy-MM-dd HH:mm:ss`
- **时区支持**：服务器本地时区
- **前端格式化**：Vue 组件统一使用 `formatDateTime` 工具函数

#### 🎨 UI/UX 设计
- **设计系统**：
  - 全站宋体字体（衬线字体）
  - 统一的设计 token（颜色、间距、圆角等）
  - 双主题支持（明亮主题 / 护眼主题）
- **响应式布局**：适配桌面端和移动端
- **交互反馈**：加载状态、成功提示、错误提示
- **无障碍**：语义化 HTML、键盘导航支持

## 🛠️ 技术栈

### 前端技术

| 技术 | 版本 | 用途 |
|------|------|------|
| **Vue 3** | 最新 | 渐进式前端框架，Composition API |
| **Vue Router** | 4.x | 单页应用路由管理 |
| **Pinia** | 最新 | 状态管理，替代 Vuex |
| **Axios** | 最新 | HTTP 客户端，API 请求 |
| **ECharts** | 5.x | 数据可视化图表库 |
| **Vite** | 最新 | 快速的前端构建工具 |
| **Playwright** | 最新 | 端到端测试（可选） |

**前端特色**：
- 组件化开发，代码复用性高
- Composition API 提升代码组织性
- Pinia 轻量级状态管理
- ECharts 丰富的图表展示
- Vite 极速的开发体验

### 后端技术

| 技术 | 版本 | 用途 |
|------|------|------|
| **Node.js** | 18+ | JavaScript 运行时 |
| **Express** | 4.x | Web 应用框架 |
| **better-sqlite3** | 最新 | SQLite 数据库驱动（同步 API） |
| **bcryptjs** | 最新 | 密码加密哈希 |
| **jsonwebtoken** | 最新 | JWT 令牌生成和验证 |
| **multer** | 最新 | 文件上传中间件 |
| **nodemailer** | 最新 | 邮件发送服务 |
| **node-cron** | 最新 | 定时任务调度 |
| **axios** | 最新 | HTTP 客户端，调用上游 API |
| **crypto** | 内置 | 加密解密工具 |

**后端特色**：
- Express 轻量级、灵活的路由设计
- SQLite 零配置、单文件数据库
- JWT 无状态身份认证
- 完整的安全机制（加密、鉴权、限流）
- 模块化代码结构，易于扩展

### 部署方案

- **Docker**：提供完整的 Docker Compose 配置
- **传统部署**：支持 PM2 进程管理
- **Nginx**：前端静态文件服务和反向代理
- **跨平台**：支持 Linux、macOS、Windows

## 📁 项目结构

### 后端

- Node.js
- Express
- better-sqlite3
- bcryptjs
- jsonwebtoken
- multer
- nodemailer
- node-cron
- axios

## 项目结构

```text
image2-ui/
├─ frontend/                    # Vue 前端
│  ├─ public/                   # 静态资源
│  ├─ src/
│  │  ├─ api/                   # API 请求封装
│  │  ├─ components/            # 可复用组件
│  │  ├─ layouts/               # 布局组件
│  │  ├─ pages/                 # 页面组件
│  │  │  ├─ Home.vue           # 首页
│  │  │  ├─ Login.vue          # 登录注册
│  │  │  ├─ Create.vue         # 图片生成
│  │  │  ├─ History.vue        # 历史记录
│  │  │  ├─ Community.vue      # 社区列表
│  │  │  ├─ CommunityDetail.vue # 帖子详情
│  │  │  ├─ Profile.vue        # 个人中心
│  │  │  └─ admin/             # 管理后台页面
│  │  ├─ router/                # 路由配置
│  │  ├─ stores/                # Pinia 状态管理
│  │  ├─ styles/                # 全局样式、设计 token、主题
│  │  └─ utils/                 # 工具函数（datetime、request 等）
│  ├─ Dockerfile                # 前端 Docker 配置
│  ├─ nginx.conf                # Nginx 配置
│  └─ package.json
├─ backend/                     # Express 后端
│  ├─ src/
│  │  ├─ config/                # 配置文件
│  │  │  └─ env.js             # 环境变量加载
│  │  ├─ controllers/           # 控制器层
│  │  │  ├─ authController.js  # 认证相关
│  │  │  ├─ adminController.js # 管理员功能
│  │  │  ├─ userController.js  # 用户功能
│  │  │  └─ ...
│  │  ├─ db/                    # 数据库相关
│  │  │  ├─ schema.sql         # 数据表定义
│  │  │  └─ database.js        # 数据库连接
│  │  ├─ middleware/            # 中间件
│  │  │  ├─ auth.js            # JWT 鉴权
│  │  │  ├─ adminAuth.js       # 管理员鉴权
│  │  │  └─ rateLimiter.js     # 限流中间件
│  │  ├─ routes/                # 路由定义
│  │  │  ├─ auth.js            # 认证路由
│  │  │  ├─ admin.js           # 管理员路由
│  │  │  ├─ user.js            # 用户路由
│  │  │  └─ ...
│  │  ├─ services/              # 业务逻辑层
│  │  │  ├─ chatgptSessionService.js  # 图片生成服务
│  │  │  ├─ linuxdoConnectService.js  # OAuth 服务
│  │  │  ├─ emailService.js    # 邮件服务
│  │  │  └─ ...
│  │  ├─ utils/                 # 工具函数
│  │  │  ├─ encryption.js      # 加密解密
│  │  │  ├─ linuxdoConnect.js  # OAuth 辅助函数
│  │  │  └─ ...
│  │  └─ server.js              # 应用入口
│  ├─ uploads/                  # 图片存储目录（运行时生成）
│  ├─ data.sqlite               # SQLite 数据库（运行时生成）
│  ├─ .env.example              # 环境变量示例
│  ├─ Dockerfile                # 后端 Docker 配置
│  └─ package.json
├─ docker-compose.yml           # Docker Compose 配置
├─ lcode-image-logo.png         # 项目 Logo
├─ index.png                    # 首页截图
├─ make-image.png               # 生成页截图
├─ community-image.png          # 社区页截图
├─ mine.png                     # 个人中心截图
└─ README.md                    # 项目文档
```

**目录说明**：
- `frontend/` - Vue 3 单页应用，使用 Vite 构建
- `backend/` - Express RESTful API 服务
- `uploads/` - 用户上传和生成的图片存储
- `data.sqlite` - SQLite 数据库文件
- Docker 相关文件支持容器化部署

## 🔌 上游接口兼容性

Lcode Image 设计为兼容 **OpenAI 风格的图片生成接口**，可以对接多种 AI 图片生成服务。

### 支持的接口

| 接口类型 | 路径 | 说明 |
|---------|------|------|
| **文生图** | `/v1/images/generations` | 根据文本提示词生成图片 |
| **图片编辑** | `/v1/images/edits` | 基于参考图和提示词编辑图片 |
| **健康检查** | `/health` | 检查上游服务可用性 |

### 默认配置

```javascript
{
  model: "gpt-image-2",        // 模型名称
  size: "1024x1024",           // 图片尺寸
  quality: "hd",               // 图片质量
  n: 1,                        // 生成数量
  style: ""                    // 风格（可选）
}
```

### 接入其他服务

只要你的上游服务兼容 OpenAI 图片生成 API 协议，就可以无缝接入。例如：
- OpenAI DALL-E API
- 自建的兼容服务
- 第三方代理服务

### 配置位置

相关配置文件：
- `backend/.env.example` - 环境变量模板
- `backend/src/config/env.js` - 配置加载逻辑
- `backend/src/services/chatgptSessionService.js` - 上游调用实现

### 自定义接口

如需对接其他协议的图片生成服务，只需修改 `chatgptSessionService.js` 中的请求格式和响应解析逻辑即可。

## ⚙️ 环境要求

| 环境 | 版本要求 | 说明 |
|------|---------|------|
| **Node.js** | 18.x 或更高 | JavaScript 运行时 |
| **npm** | 9.x 或更高 | 包管理器 |
| **操作系统** | Linux / macOS / Windows | 跨平台支持 |
| **Docker** | 20.x 或更高（可选） | 容器化部署 |
| **磁盘空间** | 建议 5GB+ | 用于存储图片和数据库 |

**推荐配置**：
- CPU: 2 核心或更多
- 内存: 2GB 或更多
- 网络: 稳定的互联网连接（用于调用上游 API）

## 🚀 快速开始

### 方式一：本地开发

#### 1️⃣ 安装依赖

```bash
# 安装前端依赖
cd frontend
npm install

# 安装后端依赖
cd ../backend
npm install
```

#### 2️⃣ 配置环境变量

复制环境变量模板：

```bash
cp backend/.env.example backend/.env
```

编辑 `backend/.env`，配置以下关键参数：

```env
# 服务端口
PORT=3001

# JWT 密钥（必须修改）
JWT_SECRET=your-super-secret-jwt-key-here

# 管理员账号（首次登录使用）
ADMIN_USERNAME=admin
ADMIN_PASSWORD=your-secure-password

# 加密密钥（必须修改，32 字符）
ENCRYPTION_SECRET=your-32-character-encryption-key

# 前端地址（用于 CORS）
FRONTEND_BASE_URL=http://127.0.0.1:5173
CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

# 上游图片 API 配置（必须配置）
IMAGE2_API_BASE_URL=https://your-image-api.com
IMAGE2_API_GENERATE_PATH=/v1/images/generations
IMAGE2_API_EDIT_PATH=/v1/images/edits
IMAGE2_API_TEST_PATH=/health
IMAGE2_API_MODEL=gpt-image-2
IMAGE2_API_SIZE=1024x1024
IMAGE2_API_QUALITY=hd
IMAGE2_API_COUNT=1

# 限流配置
DEFAULT_DAILY_LIMIT=20

# 自动清理配置（Cron 表达式）
DEFAULT_CLEANUP_CRON=0 * * * *

# 邮箱配置（可选，用于注册验证码）
EMAIL_SMTP_HOST=smtp.qq.com
EMAIL_SMTP_PORT=465
EMAIL_SMTP_SECURE=true
EMAIL_SENDER=your-email@qq.com
EMAIL_AUTH_USER=your-email@qq.com
EMAIL_AUTH_PASS=your-smtp-auth-code
```

**重要提示**：
- `JWT_SECRET` 和 `ENCRYPTION_SECRET` 必须修改为随机字符串
- `IMAGE2_API_BASE_URL` 必须配置为你的图片生成 API 地址
- 邮箱配置是可选的，不配置则无法使用邮箱注册功能

#### 3️⃣ 启动服务

启动后端：

```bash
cd backend
npm run dev
```

后端服务将运行在 `http://127.0.0.1:3001`

启动前端：

```bash
cd frontend
npm run dev
```

前端服务将运行在 `http://127.0.0.1:5173`

#### 4️⃣ 访问应用

- **前端首页**：http://127.0.0.1:5173
- **管理后台**：http://127.0.0.1:5173/admin/login
- **后端 API**：http://127.0.0.1:3001/api
- **健康检查**：http://127.0.0.1:3001/health

**默认管理员账号**：
- 用户名：`admin`
- 密码：`.env` 中配置的 `ADMIN_PASSWORD`（默认 `lcode`）

## 📦 生产部署

### 方式一：Docker Compose（推荐）

Docker 部署是最简单的生产部署方式，一键启动前后端服务。

#### 1️⃣ 准备环境文件

前端构建：

```bash
cd frontend
npm install
npm run build
```

后端运行：

```bash
cd backend
npm install
npm run start
```

推荐配合 PM2：

```bash
npm install -g pm2
cd backend
pm2 start src/server.js --name lcode-image-backend
pm2 save
pm2 startup
```

前端如果独立部署，记得配置：

```env
VITE_API_BASE_URL=https://api.your-domain.com/api
```

### 方案二：Docker

项目已内置最小可用 Docker 配置：

- `frontend/Dockerfile`
- `frontend/nginx.conf`
- `backend/Dockerfile`
- `docker-compose.yml`

部署前先准备后端环境文件：

```bash
cp backend/.env.example backend/.env
```

然后修改 `backend/.env` 里的管理员密码、JWT 密钥、加密密钥、上游图片接口和邮箱配置。

如果你修改了前端映射端口或使用域名部署，还需要同步修改 `docker-compose.yml`：

```yaml
environment:
  FRONTEND_BASE_URL: http://你的域名或服务器IP:前端端口
  CORS_ORIGINS: http://你的域名或服务器IP:前端端口
ports:
  - "前端端口:80"
```

例如前端对外端口是 `8080`，`CORS_ORIGINS` 就需要包含 `http://localhost:8080`、`http://服务器IP:8080` 或你的正式域名。否则浏览器访问前端后请求后端 API 时会被 CORS 拦截。多个来源用英文逗号分隔，不要写多余空格。

如果首次启动前本地没有数据库文件和上传目录，可以先创建：

```bash
touch backend/data.sqlite
mkdir -p backend/uploads
```

启动：

```bash
docker compose -p lcode-image up --build -d
```

默认地址：

- 前端：`http://127.0.0.1:8080`
- 后端 API：`http://127.0.0.1:3002/api`
- 后端健康检查：`http://127.0.0.1:3002/health`

说明：

- 容器内后端实际使用 `/app/backend/data.sqlite` 与 `/app/backend/uploads`，因此 compose 中也需要挂载到这两个路径
- 浏览器访问前端时，前端会通过 Nginx 反向代理把 `/api`、`/uploads`、`/health` 转发到容器内后端
- 因此公开访问前端时不需要把浏览器请求直接打到 `localhost:3002`

常用命令：

```bash
docker compose -p lcode-image ps
docker compose -p lcode-image logs -f backend
docker compose -p lcode-image logs -f frontend
docker compose -p lcode-image down
```

## 管理员初始化

### 默认管理员账号

- 用户名：`admin`
- 初始密码：
  - 如果你在 `backend/.env` 中设置了 `ADMIN_PASSWORD`，则以你的配置为准
  - 如果没有设置，代码默认回退值为 `lcode`

> 首次登录后建议立即修改管理员密码。

首次部署后建议按这个顺序完成：

1. 用 `.env` 里的管理员账号登录后台
2. 修改管理员密码
3. 进入 `/admin/config`
4. 配置图片 API 地址
5. 配置共享身份令牌
6. 配置站点 URL
7. 如需邮箱注册，配置 SMTP
8. 如需邀请码，开启并生成邀请码
9. 如需 Linux.do 一键登录，填写 Client ID / Secret / 回调地址
10. 配置首页示例灵感池

## Linux.do OAuth 接入

支持以 Linux.do 账号一键登录。流程：

1. 在 Linux.do 应用后台创建 OAuth 应用，填写回调地址：`https://你的域名/api/auth/linuxdo/callback`
2. 进入本站 `/admin/config` → 「Linux.do 接入配置」
3. 填写 Client ID、Client Secret、回调地址（与 Linux.do 后台保持一致），保存即可
4. 用户在 `/login` 页面会自动出现「使用 Linux.do 登录」按钮

实现要点：

- 授权请求由前端通过后端 `/api/auth/linuxdo/authorize` 发起，后端生成 state 并返回授权 URL
- Linux.do 回调由后端处理（`/api/auth/linuxdo/callback`），用 code 换 token、拉取用户信息后 upsert 本地用户
- `client_secret` 始终存放在后端并加密入库，绝不下发到前端
- 已有同邮箱账号会自动绑定到 Linux.do 用户 ID；无对应账号则自动创建

## 默认页面路由

- `/` 首页
- `/login` 用户登录 / 注册
- `/oauth/linuxdo/callback` Linux.do 登录回调落地页
- `/create` 开始生成
- `/history` 历史记录
- `/community` 社区列表
- `/community/:id` 帖子详情
- `/profile` 用户主页
- `/admin/login` 管理员登录
- `/admin` 管理后台首页
- `/admin/config` 管理员配置中心
- `/admin/users` 用户管理
- `/admin/images` 图片资源管理

## 数据表概览

核心表包括：

- `admin_settings`
- `users`（含 `linuxdo_user_id` 唯一索引）
- `user_profiles`
- `generated_images`
- `generation_logs`
- `community_posts`
- `avatar_previews`
- `featured_prompts`
- `featured_examples`
- `email_verification_codes`
- `invite_codes`
- `linuxdo_connect`（Linux.do OAuth 配置，client_secret 加密存储）

定义位于：

- `backend/src/db/schema.sql`

## 安全说明

当前已具备的基础能力：

- 管理员 / 用户 JWT 鉴权
- bcrypt 密码哈希
- 敏感令牌加密存储
- 注册策略与邀请码控制
- 单 IP 限流
- Prompt 校验

如果要正式公网部署，仍建议继续增强：

- 更严格的 CORS 白名单
- HTTPS
- 更强的管理员密码策略
- 行为审计与日志留存
- 备份策略
- 更多滥用防护

## 发布前检查清单

- [ ] 确认已修改 `backend/.env` 中的管理员密码、JWT 密钥、加密密钥
- [ ] 确认没有提交真实 token、SMTP 授权码、数据库和上传文件
- [ ] 确认 `site_base_url`、CORS、前端公开地址一致
- [ ] 手动验证首页、生成页、历史页、社区页、个人主页、管理员后台
- [ ] 补充截图、演示地址、仓库地址

## 常用命令

前端：

```bash
cd frontend
npm run dev
npm run build
npm run preview
```

后端：

```bash
cd backend
npm run dev
npm run start
```

## 最后，我的服务器应该是被L站封IP了，本人也不确定现在是否是打通了oauth单点，代码上看的话是没有问题的，待测试。

## License

MIT

---

# English

## Overview

**Lcode Image** is a self-hostable AI image generation web app with user accounts, community sharing, avatar generation, admin controls, invite-code registration, and email verification.

It is suitable for private deployment, secondary development, and open-source release.

## Feature Preview

### Landing Page

![Landing page preview](./index.png)

### Create Page

![Create page preview](./make-image.png)

### Community

![Community page preview](./community-image.png)

### Profile

![Profile page preview](./mine.png)

## Highlights

- Guest mode and authenticated user mode
- Text-to-image and reference-image editing
- Image history with automatic expiration cleanup
- Community posts with detail pages
- User profile page with avatar upload and AI avatar preview
- Admin dashboard for upstream config, site URL, announcements, users, invite codes, and image management
- Dedicated `/admin/config` page that consolidates all runtime configuration
- Featured homepage prompt pool and rotating example image
- Email-based registration and password reset
- One-click sign-in with **Linux.do** OAuth (configured by admin)
- Unified design tokens and dual-theme (light / eye-care) styling, consistent date formatting

## Stack

- Frontend: Vue 3, Vue Router, Pinia, Axios, ECharts, Vite, Playwright
- Backend: Node.js, Express, better-sqlite3, bcryptjs, jsonwebtoken, multer, nodemailer, node-cron, axios

## Quick Start

Copy the backend environment example first:

```bash
cp backend/.env.example backend/.env
```

Install dependencies:

```bash
cd backend && npm install
cd ../frontend && npm install
```

Configure `backend/.env`:

```env
PORT=3001
JWT_SECRET=change-this-secret
ADMIN_USERNAME=admin
ADMIN_PASSWORD=change-me
ENCRYPTION_SECRET=change-this-encryption-secret-32
CHATGPT_SESSION_URL=https://chatgpt.com/api/auth/session
FRONTEND_BASE_URL=http://127.0.0.1:5173
CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

IMAGE2_API_BASE_URL=
IMAGE2_API_GENERATE_PATH=/v1/images/generations
IMAGE2_API_EDIT_PATH=/v1/images/edits
IMAGE2_API_TEST_PATH=/health
IMAGE2_API_MODEL=gpt-image-2
IMAGE2_API_SIZE=1024x1024
IMAGE2_API_QUALITY=hd
IMAGE2_API_COUNT=1
IMAGE2_API_STYLE=

DEFAULT_DAILY_LIMIT=20
DEFAULT_CLEANUP_CRON=0 * * * *

EMAIL_SMTP_HOST=smtp.qq.com
EMAIL_SMTP_PORT=465
EMAIL_SMTP_SECURE=true
EMAIL_SENDER=
EMAIL_AUTH_USER=
EMAIL_AUTH_PASS=
```

Run locally:

```bash
cd backend
npm run dev
```

```bash
cd frontend
npm run dev
```

Default local addresses:

- Frontend: `http://127.0.0.1:5173`
- Backend: `http://127.0.0.1:3001`

## Docker

The repository includes a minimal Docker setup:

- `frontend/Dockerfile`
- `frontend/nginx.conf`
- `backend/Dockerfile`
- `docker-compose.yml`

Build and start with one command:

Before deploying, prepare the backend environment file:

```bash
cp backend/.env.example backend/.env
```

Then update the admin password, JWT secret, encryption secret, upstream image API, and email settings in `backend/.env`.

If you change the public frontend port or deploy with a domain name, also update `docker-compose.yml`:

```yaml
environment:
  FRONTEND_BASE_URL: http://your-domain-or-server-ip:frontend-port
  CORS_ORIGINS: http://your-domain-or-server-ip:frontend-port
ports:
  - "frontend-port:80"
```

For example, if the frontend is exposed on port `8080`, `CORS_ORIGINS` must include `http://localhost:8080`, `http://your-server-ip:8080`, or your production domain. Otherwise browser API requests from the frontend may be blocked by CORS. Use commas to separate multiple origins, without extra spaces.

If the local database file and upload directory do not exist before the first start, create them first:

```bash
touch backend/data.sqlite
mkdir -p backend/uploads
```

Start the stack:

```bash
docker compose -p lcode-image up --build -d
```

Default container addresses:

- Frontend: `http://127.0.0.1:8080`
- Backend API: `http://127.0.0.1:3002/api`
- Backend health check: `http://127.0.0.1:3002/health`

Notes:

- When users open the frontend, Nginx proxies `/api`, `/uploads`, and `/health` to the backend container
- Browsers should access the deployed site through the frontend address instead of calling `localhost:3002` directly

## Default Admin Account

- Username: `admin`
- Initial password:
  - uses `ADMIN_PASSWORD` from `backend/.env` when provided
  - otherwise falls back to `lcode` in code

> Change the admin password immediately after the first login.

## Default Routes

- `/`
- `/login`
- `/oauth/linuxdo/callback`
- `/create`
- `/history`
- `/community`
- `/community/:id`
- `/profile`
- `/admin/login`
- `/admin`
- `/admin/config`
- `/admin/users`
- `/admin/images`

## Linux.do OAuth

Sign in with a Linux.do account. Steps:

1. Create an OAuth app on Linux.do, set the redirect URI to `https://your-domain/api/auth/linuxdo/callback`
2. Open `/admin/config` → "Linux.do connect"
3. Fill in Client ID, Client Secret, and the redirect URL (must match the Linux.do app), then save
4. The "Sign in with Linux.do" button will appear on `/login`

Notes:

- Authorization URL is generated by the backend (with state) via `/api/auth/linuxdo/authorize`
- Token exchange and user fetch happen in the backend; `client_secret` is encrypted at rest and never reaches the browser
- An existing local account with the same email is auto-linked to the Linux.do user id; otherwise a new user is created

## Notes

- Site image URLs can be normalized through the admin-configured `site_base_url`
- Community posts expire together with their related generated images
- Avatars do not participate in the 3-day cleanup lifecycle

## Release Checklist

- [ ] Replace the admin password, JWT secret, and encryption secret in `backend/.env`
- [ ] Make sure no real tokens, SMTP credentials, database files, or uploads are committed
- [ ] Verify that `site_base_url`, CORS, and public frontend URLs match
- [ ] Manually test the landing, create, history, community, profile, and admin pages
- [ ] Add final screenshots, demo URL, and repository URL

## License

MIT

---

# 日本語

## 概要

**Lcode Image** は、ユーザーアカウント、コミュニティ投稿、アバター生成、管理画面、招待コード登録、メール認証を備えたセルフホスト向け AI 画像生成 Web アプリです。

個人運用、二次開発、オープンソース公開に適しています。

## 画面プレビュー

### ホーム

![ホーム画面プレビュー](./index.png)

### 画像生成

![画像生成ページプレビュー](./make-image.png)

### コミュニティ

![コミュニティページプレビュー](./community-image.png)

### プロフィール

![プロフィールページプレビュー](./mine.png)

## 主な機能

- ゲストモードとユーザーモード
- テキストから画像生成、参照画像の編集
- 履歴保存と自動期限切れクリーンアップ
- 詳細ページ付きのコミュニティ投稿
- アバターのアップロードと AI 生成プレビュー
- 上流設定、サイト URL、お知らせ、ユーザー、招待コード、画像管理ができる管理画面
- すべての設定を集約した `/admin/config` ページ
- ホーム用の注目プロンプトとサンプル画像ローテーション
- メール認証による登録とパスワード再設定
- **Linux.do** OAuth ワンクリックログイン（管理者が設定）
- 統一デザイントークンとライト / 目に優しい二つのテーマ、日時表示の統一

## 技術スタック

- フロントエンド: Vue 3, Vue Router, Pinia, Axios, ECharts, Vite, Playwright
- バックエンド: Node.js, Express, better-sqlite3, bcryptjs, jsonwebtoken, multer, nodemailer, node-cron, axios

## クイックスタート

まずバックエンド環境変数のサンプルをコピーします。

```bash
cp backend/.env.example backend/.env
```

依存関係をインストールします。

```bash
cd backend && npm install
cd ../frontend && npm install
```

`backend/.env` を設定してから起動します。特に管理者パスワード、JWT シークレット、暗号化キー、上流画像 API、メール設定を確認してください。

```bash
cd backend
npm run dev
```

```bash
cd frontend
npm run dev
```

ローカルのデフォルト URL:

- フロントエンド: `http://127.0.0.1:5173`
- バックエンド: `http://127.0.0.1:3001`

## Docker

最小構成の Docker セットアップが含まれています。

- `frontend/Dockerfile`
- `frontend/nginx.conf`
- `backend/Dockerfile`
- `docker-compose.yml`

1 行でビルドと起動ができます。

デプロイ前にバックエンド環境変数ファイルを用意します。

```bash
cp backend/.env.example backend/.env
```

`backend/.env` の管理者パスワード、JWT シークレット、暗号化キー、上流画像 API、メール設定を変更してください。

フロントエンドの公開ポートを変更する場合やドメインで公開する場合は、`docker-compose.yml` も更新してください。

```yaml
environment:
  FRONTEND_BASE_URL: http://ドメインまたはサーバーIP:フロントエンドポート
  CORS_ORIGINS: http://ドメインまたはサーバーIP:フロントエンドポート
ports:
  - "フロントエンドポート:80"
```

例えばフロントエンドの公開ポートが `8080` の場合、`CORS_ORIGINS` には `http://localhost:8080`、`http://サーバーIP:8080`、または本番ドメインを含める必要があります。含まれていないと、ブラウザからの API リクエストが CORS でブロックされることがあります。複数指定する場合はカンマ区切りにし、余分なスペースは入れないでください。

初回起動前にローカルのデータベースファイルとアップロードディレクトリがない場合は、先に作成します。

```bash
touch backend/data.sqlite
mkdir -p backend/uploads
```

起動：

```bash
docker compose -p lcode-image up --build -d
```

デフォルトの公開先:

- フロントエンド: `http://127.0.0.1:8080`
- バックエンド API: `http://127.0.0.1:3002/api`
- ヘルスチェック: `http://127.0.0.1:3002/health`

補足:

- フロントエンドにアクセスすると、Nginx が `/api`、`/uploads`、`/health` をバックエンドコンテナへリバースプロキシします
- ブラウザからは `localhost:3002` を直接叩かず、公開されているフロントエンド URL を利用してください

## 初期管理者アカウント

- ユーザー名: `admin`
- 初期パスワード:
  - `backend/.env` に `ADMIN_PASSWORD` を設定した場合はその値
  - 未設定の場合はコード上の既定値 `lcode`

> 初回ログイン後に管理者パスワードをすぐ変更してください。

## 主要ルート

- `/`
- `/login`
- `/oauth/linuxdo/callback`
- `/create`
- `/history`
- `/community`
- `/community/:id`
- `/profile`
- `/admin/login`
- `/admin`
- `/admin/config`
- `/admin/users`
- `/admin/images`

## Linux.do OAuth 連携

Linux.do アカウントでログインできます。

1. Linux.do の管理画面で OAuth アプリを作成し、リダイレクト URI を `https://あなたのドメイン/api/auth/linuxdo/callback` に設定
2. 本サイトの `/admin/config` →「Linux.do 接入配置」
3. Client ID、Client Secret、コールバック URL（Linux.do 側と一致させる）を入力して保存
4. `/login` に「Linux.do でログイン」ボタンが表示されます

ポイント:

- 認可 URL は state 付きでバックエンドが発行（`/api/auth/linuxdo/authorize`）
- トークン交換とユーザー情報取得はバックエンドで完結し、`client_secret` は暗号化保存されブラウザに渡りません
- 同じメールのローカルアカウントがあれば自動で Linux.do user id に紐付け、なければ新規作成します

## 補足

- 画像 URL は管理者が設定した `site_base_url` を通して統一できます
- コミュニティ投稿は関連する生成画像と一緒に期限切れになります
- アバターは 3 日の自動削除対象になりません

## 公開前チェックリスト

- [ ] `backend/.env` の管理者パスワード、JWT シークレット、暗号化キーを変更する
- [ ] 実際のトークン、SMTP 認証情報、データベース、アップロード画像がコミットされていないことを確認する
- [ ] `site_base_url`、CORS、公開用フロントエンド URL が一致していることを確認する
- [ ] ホーム、生成、履歴、コミュニティ、プロフィール、管理画面を手動確認する
- [ ] 最終スクリーンショット、デモ URL、リポジトリ URL を追加する

## License

MIT
